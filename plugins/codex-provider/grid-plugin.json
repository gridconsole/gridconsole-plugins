{
  "id": "codex-provider",
  "name": "codex-provider",
  "version": "0.1.0",
  "apiVersion": ">=0.1",
  "publisher": "grid console",
  "description": "Sessions through Codex CLI, and the stage prompts the default SDLC sends when it starts or resumes one.",
  "stability": {
    "level": "unstable",
    "note": "Its stage prompts have had far less real use than the Claude provider's."
  },
  "permissions": {
    "network": [],
    "cardWrite": false,
    "filesystem": [],
    "shell": false
  },
  "hooks": [],
  "points": [
    "agent.provider",
    "llm.provider"
  ],
  "activation": [],
  "configuration": {
    "prompts.prepare": {"type": "prompt", "title": "Prepare prompt", "default": "Settings › SDLC"},
    "prompts.start": {"type": "prompt", "title": "Start prompt (no prepare stage)", "default": "Settings › SDLC"},
    "prompts.build": {"type": "prompt", "title": "Build prompt", "default": "Settings › SDLC"},
    "prompts.review": {"type": "prompt", "title": "Self-review prompt", "default": "Settings › SDLC"},
    "prompts.deliver": {"type": "prompt", "title": "Deliver prompt", "default": "Settings › SDLC"},
    "prompts.verify": {"type": "prompt", "title": "Verify prompt", "default": "Settings › SDLC"},
    "defaultModel": {"type": "string", "title": "Default model", "default": ""},
    "sandbox": {"type": "enum", "title": "Sandbox", "default": "workspace-write", "options": ["read-only", "workspace-write", "danger-full-access"]},
    "mcpConfig": {"type": "file", "title": "MCP servers", "default": ".codex/config.toml"}
  },
  "main": "index.js"
}
